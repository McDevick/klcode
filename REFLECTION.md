# REFLECTION：从 Superpowers/subagent 到稳定产品工程

## 1. 结论先写

这轮项目最深的体会是：**AI 是放大器，不是稳定器。稳定的产品不在 AI 手上，而是在工程师的大脑里。**

有用的部分很明确：

1. **TDD 绝对是最有用的。** 它把“我觉得完成了”变成“测试证明没破坏”，尤其适合修 bug 时保护已有功能。
2. **Superpowers 前期走骨架可以，后期修 bug 还是自由一些才好。** 稳定体验不是靠一堆流程模板堆出来的，过度约束反而降低效率。
3. **subagent 可以用于边界清晰的小任务，但相比 subagent，用两个独立 agent 分工干效果更好。** 例如 Claude Code 负责审查计划，Codex 负责执行；审查和执行分离，比同一上下文里的子任务更不容易互相污染。
4. **机制细节必须靠人思考。** AI 很少主动考虑“服务端是不是全局的”“启动会不会产生副作用”“没有服务端时 TUI 该怎么启动”“README 应该怎么组织、读者会遇到什么问题”。这些不是代码写出来就自动成立的。

## 2. AI 的典型问题

### 2.1 没说清楚时，AI 容易选简单解法

只要 prompt 没有明确要求，AI 很容易采用看起来“简单”但并不可靠的解法。典型表现是：

- 遇到上下文问题，直接引入 skill / 上下文机制；
- 遇到工具调用问题，先加一层自己的封装；
- 遇到模型通信，绕开官方 SDK，直接用 httpx 手写请求。

这些解法短期能跑，但往往忽略协议边界、错误处理、版本兼容和可维护性。**AI 不会主动告诉你“这里我有更简单但更脆弱的假设”，这需要人去发现和追问。**

### 2.2 容易出现“过度详细”或“绕路实现”

一个很典型的案例是：明明应该使用 OpenAI 官方库，结果自己用 httpx 写和 LLM 的通信。这样看起来更“可控”，实际会带来大量 bug：

- 消息格式不一致；
- 工具调用结构不兼容；
- 重试、超时、错误分类都退化成手写逻辑；
- 后续升级 SDK 或模型能力时无法复用生态。

工程上应该优先使用成熟库，除非有明确理由自己实现。**AI 的“详细”不等于“正确”，过度详细往往是为了绕过不确定性，而不是解决不确定性。**

### 2.3 AI 很难发现机制层面的细节问题

AI 擅长在已有代码上做局部修改，但很难主动发现跨模块、跨启动流程的机制问题。例如：

- server 是全局 daemon，配置/数据库/记忆是否真的全局化；
- 在任意目录启动 TUI，会不会在项目目录创建副作用；
- 用户只装了 CLI、没有装 server，首次启动应该怎么处理；
- README 从用户视角是否真的能看懂；
- 发布包里的安装路径、命令入口、依赖下限是否齐全。

这些问题的共同点是：**只有站在“一个全新用户/全新机器”的角度去推演，才能暴露出来。** AI 很少主动做这种推演，所以 README、发布、启动流程、生命周期这类内容必须由人把关。

## 3. TDD 为什么最有用

TDD 的价值不只是“先写测试再写实现”，而是给 AI 一个不可抵赖的验收锚点。

- 新功能：红到绿，证明行为符合预期。
- 修 bug：先写失败测试，再修到通过，保证不会“修好 A 又弄坏 B”。
- 重构：测试保证现有功能不被破坏，AI 才敢放心改代码。
- 文档/构建任务：用“验证命令先失败，再修到通过”代替“读一遍看起来没问题”。

在分发和发布任务上尤其明显：`npm pack --dry-run`、`python -m build server`、版本一致性检查，都比评审文字更能暴露真实问题。**TDD 提高了 AI 的可控性，因为它把“完成”从主观判断变成客观证据。**

## 4. Superpowers 的取舍

Superpowers 的骨架在前期是有效的：

- 强制拆 task；
- 强制写验证命令；
- 强制用 worktree / PR 保留边界；
- 强制记录 AGENT_LOG，出错时能定位是哪一步偏离。

但它的“稳定体验”并不好用。实际过程中出现过：

- 单一会话承担多个 task，规则失守；
- 共享 AGENT_LOG 导致并行 worktree 被迫改成 stacked；
- 文档/构建任务只走评审模板，没有绑定命令就变成形式；
- 对后期修 bug 来说，流程约束太多，反而影响效率。

我的结论是：**前期用 Superpowers 走骨架可以，后期修 bug 还是自由一些才好。** 关键是保留它真正有用的部分：任务边界、验证证据、审计记录。模板本身不是质量保证。

## 5. subagent 与独立 agent 分工

subagent-driven development 在小 task 上有效：每个 implementer 有独立 worktree、独立 diff、独立验证，出错时容易定位。

但跨 task 后问题很明显：

- 同一上下文承担太多任务会漂移；
- 子 agent 共享历史越多，越容易把“上一件事”的假设带进来；
- 如果让同一个 agent 既设计又执行，很容易自己验证自己的方案，形成盲区。

所以我现在的判断是：**相比 subagent，用两个独立 agent 分工干效果更好。** 一个负责审查计划、推演风险，另一个负责执行；审查和执行分离，等于给流程增加了一个外部视角。例如 Claude Code 审查计划，Codex 执行实现；即使两者共享仓库，也不共享同一个上下文和同一套“自我确认”。

## 6. 流程与任务粒度

最优 task 大约是：

```text
1-2 小时
一个文件集
一个可验证结果
```

越小的 task，越容易让 agent 自主完成；越大的 phase，越容易出现“看起来在做，其实已经偏了”。

SPEC/PLAN 质量也很关键：

- SPEC 承诺、PLAN 任务、验证命令三者必须双向核对；
- PLAN 要包含“明确不做”清单；
- prompt 只需要当前 task 的文件范围、验证命令和边界，不需要完整历史。

实际的偏离案例：

- Task 4.7 把 `.kl/tools/<name>/` 目录契约实现成扁平文件；
- Task 5.2 README 写了 `pytest --pyargs`，和 Makefile 不一致；
- Task 5.6 一直 pending，说明 PLAN 缺了 bootstrap 任务：模块各自可测，不等于应用能被组装起来。

这些都不是“代码写错”，而是**上下文里的契约没有被双向验证**。评审必须绑定执行命令，否则只是再读一遍。

## 7. 系统设计教训

### 7.1 daemon 生命周期是“启动方式决定归属”

```text
用户手动 kl server start → manual，用户自己管理
TUI/run/init 自动拉起     → auto，空闲自动回收
```

最容易出错的不是启动本身，而是：

- stale PID：进程死了但 pid 文件还在；
- 来源接管：auto 存活时手动 start，不能直接杀，要区分有无运行任务；
- 空闲回收：必须同时检查运行任务和 WS 连接。

生命周期代码表面是 PID 管理，实际是用户心智模型的管理。

### 7.2 自动拉起最容易踩的是环境漂移

“能导入 kl_server”不等于“用的是当前代码”。实际遇到 PATH 里的系统 Python 导入了旧 worktree 的 `kl_server`，导致自动服务端跑旧代码。

规则：

1. 优先使用全局 `~/.kl/venv`；
2. 源码场景 PATH 探测必须校验 `kl_server.__file__`；
3. 自举前清理损坏的 venv；
4. 排障先看 `~/.kl/daemon.log` 和 `daemon.json`。

### 7.3 全局化不是“把路径改成 home 就结束”

全局化要考虑：

- 启动有没有副作用；
- 旧数据怎么办；
- 哪些内容按项目走；
- 服务端在任意目录启动时会不会创建项目 `.kl/skills` / `.kl/tools`。

实际结果：配置、数据库、审计、记忆、skill、用户工具都放 `~/.kl/`，项目规则只保留 `.kl/rules.md` 和 `AGENTS.md`。

### 7.4 上下文压缩是有损摘要，不是无损压缩

压缩后模型保留的是结构化摘要和少量原始窗口，不是完整历史。关键路径、命令、失败信息必须进摘要或 tool_outputs 引用，否则恢复不了。

### 7.5 MCP 也要考虑 workspace 和全局性

MCP filesystem 不能依赖 server 启动目录，必须按 session workspace 启动 transport。否则全局 daemon 下，allowed directories 为空，MCP 工具永远无法访问 TUI 当前目录。

## 8. README / 发布类工作必须由人推演

README 不是“把功能列出来”，而是回答读者的问题：

- 用户怎么安装？
- 用户只装 CLI，没有装 server，怎么启动？
- 源码开发者怎么跑？
- 配置放哪里？
- demo 在哪？
- 发布包和 GitHub Release 有什么关系？

这次 README 的问题就是早期写得太像内部笔记：用了绝对路径、没有发布后启动方式、没有从“全新用户”视角检查。**这类问题 AI 很难自己发现，因为 AI 缺少“第一次使用”的体验。** 必须由人假设自己是一个新用户，一步步走一遍。

稳定产品来自反复的真实使用，而不是“AI 说完成”。